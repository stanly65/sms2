<legend>Order Summary</legend>

<table class="table table-hover">
    <tbody>
    {% for item in items %}
        <tr>
            <td>{{ item.product.title }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
