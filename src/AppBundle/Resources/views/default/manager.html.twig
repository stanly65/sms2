{% extends 'base.html.twig' %}

{% block body %}

<h1>Store Manager</h1>

<div class="row">
    <div class="col-lg-6">
        <div class="stacked button-group">
            <a href="{{ path('category_index') }}" class="btn btn-lg btn-success">Manage Categories</a>
            <br><br>
            <a href="{{ path('product_index') }}" class="btn btn-lg btn-success">Manage Products</a>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="stacked button-group">
            <a href="{{ path('salesorder_index') }}" class="btn btn-lg btn-info">Manage Orders</a>
        </div>
    </div>
</div>
{% endblock %}